# 相切圆弧波浪算法设计

## 问题描述

用户需要在灯罩的下口圆弧上创建波浪形边缘，波浪由相切的圆弧组成，而不是正弦函数的径向偏移。

## 设计目标

1. 将下口圆弧均分成N段（N = 波浪数）
2. 每段由两个相切的圆弧组成：波谷圆弧 + 波峰圆弧
3. 波谷半径由用户指定
4. 波峰半径自动计算，确保与波谷相切且满足波高要求
5. 左右边缘对称（展开图卷起后能无缝对接）

## 几何原理

### 坐标系统
- 原点：圆锥顶点
- 展开图是一个扇形环，内半径 R_inner，外半径 R_outer
- 扇形角度：θ_sector

### 波浪结构
对于N个波浪，将扇形角度均分成N段，每段角度 = θ_sector / N

每段包含：
- **波谷圆弧**：半径 r_trough（用户指定），圆心在基准圆的内侧
- **波峰圆弧**：半径 r_peak（计算得出），圆心在基准圆的外侧

### 关键约束

1. **波高定义**：从波谷最低点到波峰最高点的径向距离 = h
   - 波谷最低点到原点的距离：R_outer - h/2
   - 波峰最高点到原点的距离：R_outer + h/2

2. **相切条件**：波谷圆弧和波峰圆弧在切点处相切
   - 切点处两圆弧的切线方向相同
   - 两圆心和切点三点共线

3. **对称性**：第一段和最后一段的波形相同
   - 可以通过让第一段从波谷中点开始，最后一段在波谷中点结束来实现

## 算法步骤

### 步骤1：均分角度
```
segment_angle = θ_sector / N
```

### 步骤2：每段的结构
每段从角度 θ_start 到 θ_end，包含：
- 左侧直线：从内圆到波谷起点
- 波谷圆弧：从波谷起点到切点
- 波峰圆弧：从切点到波峰终点
- 右侧直线：从波峰终点到内圆

### 步骤3：计算波谷圆弧参数
给定：
- 基准半径：R_base = R_outer
- 波谷半径：r_trough（用户输入）
- 波高：h（用户输入）

波谷最低点到原点的距离：R_min = R_base - h/2

波谷圆心到原点的距离：
```
R_trough_center = R_min + r_trough
```

### 步骤4：计算波峰圆弧参数
波峰最高点到原点的距离：R_max = R_base + h/2

设波峰半径为 r_peak，波峰圆心到原点的距离为 R_peak_center。

根据相切条件，需要满足：
```
R_peak_center = R_max - r_peak
```

同时，切点必须在两个圆弧上，且两圆心和切点共线。

### 步骤5：简化方案
为了简化计算，我们可以假设：
- 波谷圆弧占据每段的一半角度
- 波峰圆弧占据每段的另一半角度
- 切点在段的中间角度位置

这样，对于每段：
- 波谷圆弧：从 θ_start 到 θ_mid
- 波峰圆弧：从 θ_mid 到 θ_end

其中 θ_mid = (θ_start + θ_end) / 2

## 实现计划

1. 修改 `waveformLampshadeCalculator.ts`：添加相切圆弧计算逻辑
2. 修改 `dxfExporter.ts`：使用新的圆弧参数生成DXF
3. 修改 `LampshadeVisualization.tsx`：使用新的圆弧参数渲染展开图

## 注意事项

1. 需要验证波谷半径和波高的组合是否可行（避免圆弧重叠或不相交）
2. 需要确保左右边缘对称（第一个和最后一个波谷/波峰在相同位置）
3. DXF导出时使用ARC实体，不是LINE或POLYLINE
