# 波浪形灯罩方案B设计：波浪延伸到左右边缘

## 核心思想

左右边缘不是直线，而是波浪曲线的一部分。为了确保左右边缘对称（卷起来能对接），需要：

1. **波浪从"中性点"开始和结束**：例如从波谷的最低点开始，到波谷的最低点结束
2. **左右边缘在相同的径向距离**：第一个点和最后一个点到原点的距离相同

## 算法设计

### 步骤1：确定波浪的起止相位

假设我们要4个完整波浪，每个波浪由一个波谷圆弧和一个波峰圆弧组成。

- 起点：第一个波谷圆弧的最低点（径向距离 = R_base - h/2）
- 终点：最后一个波谷圆弧的最低点（径向距离 = R_base - h/2）

这样左右边缘都在波谷的最低点，径向距离相同，可以完美对接。

### 步骤2：计算圆弧圆心位置

将扇形角度均分成 2N 段（N = 波浪数），每段对应一个半波（波谷或波峰）。

对于第 i 个半波（i = 0, 1, 2, ..., 2N-1）：
- 中心角度：θ_i = startAngle + (i + 0.5) * (sectorAngle / 2N)
- 如果 i 是偶数：波谷圆弧，圆心在基准圆内侧
- 如果 i 是奇数：波峰圆弧，圆心在基准圆外侧

### 步骤3：计算圆弧的起止点

- **第一个圆弧（i=0）的起点**：波谷圆弧的最低点
  - 角度 = 圆心角度
  - 距离 = R_base - h/2
  - 坐标 = (距离 * cos(角度), 距离 * sin(角度))

- **中间圆弧的起止点**：通过两圆交点计算

- **最后一个圆弧（i=2N-1）的终点**：波谷圆弧的最低点
  - 角度 = 圆心角度
  - 距离 = R_base - h/2
  - 坐标 = (距离 * cos(角度), 距离 * sin(角度))

### 步骤4：确保对称性

为了确保左右边缘对称，需要：
- 第一个半波是波谷（i=0，偶数）
- 最后一个半波也是波谷（i=2N-1，奇数）

所以 2N-1 必须是偶数，即 2N 必须是奇数？不对，这不可能。

让我重新思考：如果要4个完整波浪，应该有8个半波（0-7）。
- i=0: 波谷
- i=7: 波峰

这不对称！

**解决方案**：使用 2N+1 个半波，这样：
- i=0: 波谷（起点在波谷最低点）
- i=1: 波峰
- i=2: 波谷
- ...
- i=2N: 波谷（终点在波谷最低点）

这样有 N 个完整波浪 + 两个半波谷（左右边缘各一个）。

或者更简单的方案：
- 起点和终点都在波谷和波峰之间的"过渡点"（基准圆上）
- 这样左右边缘都在基准圆上，径向距离 = R_base
