# 波浪形灯罩对称性修复 - 最终方案

## 当前问题
使用2N+1个半波（9个）实现左右对称，但导致：
- 波浪数量从4个变成4.5个
- 扇形角度增大（从120°变成135°）
- 下口周长增加

## 正确方案
保持2N个半波（8个，对应4个完整波浪），通过**相位偏移**让左右边缘对称。

### 方案：对称波浪
- 使用标准的2N个半波（8个）
- 波浪从相位 `π/4` 开始（波峰的上升沿）
- 波浪在相位 `2Nπ + π/4` 结束（波峰的上升沿）
- 左右边缘都在波峰的同一位置，高度相同，形状镜像对称

### 实现细节
```typescript
// 相位偏移：让波浪从π/4开始（波峰上升沿）
const phaseOffset = Math.PI / 4;
const phase = t * 2 * waveCount * Math.PI + phaseOffset;
const offset = (waveHeight / 2) * Math.sin(phase);
```

这样：
- t=0时：phase=π/4，sin(π/4)=0.707（波峰上升沿）
- t=1时：phase=2Nπ+π/4，sin(2Nπ+π/4)=0.707（波峰上升沿）
- 左右边缘高度相同，形状相同

## 修改清单
- [ ] 修改DXF导出器：改回2N个半波，添加相位偏移π/4
- [ ] 修改展开图可视化：同步相位偏移
- [ ] 测试验证：确保左右边缘高度相同且扇形角度正确
