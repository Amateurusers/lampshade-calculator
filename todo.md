# 波浪形灯罩 DXF 修复

## 问题分析

### 问题1：波浪高度不对
- 用户输入 waveHeight=10mm，期望波浪从基准弧线向外偏移最大10mm（即峰谷差=10mm）
- 当前代码：`r = baseR + waveHeight * sin(...)` 
- sin 范围是 [-1, 1]，所以实际偏移范围是 [-waveHeight, +waveHeight]
- 峰谷差 = 2 * waveHeight = 20mm，是用户期望的两倍！
- **修复**：使用 `waveHeight/2 * sin(...)` 或者让波浪只向一个方向偏移

实际上看用户的白色图，波浪是在基准弧线两侧对称分布的，但峰谷差=7mm。
所以正确的做法是：`r = baseR + (waveHeight/2) * sin(...)`，这样峰谷差就等于 waveHeight。

### 问题2：波浪没有均布
- 当前代码用角度 t 均匀分布：`sin(t * waveCount * 2π)`
- 在扇形环中，弧长 = radius × angle
- 外弧半径大，同样角度变化对应更长的弧长
- 内弧半径小，同样角度变化对应更短的弧长
- 所以按角度均匀分布时，波浪在外弧上看起来稀疏（弧长长），在内弧上看起来密集（弧长短）

**但是**：对于同一条弧线（比如只看外弧），角度均匀分布 = 弧长均匀分布（因为半径恒定）。
所以问题不在于角度vs弧长，而是波浪的偏移方向是径向的，导致在不同角度位置波浪的视觉大小不同。

等等，让我重新分析。看用户的截图：
- 白色（正确）：波浪在整个弧线上大小一致，分布均匀
- 红色（我的）：波浪在左侧和右侧大小不一致

实际上对于单条弧线，如果波浪是沿径向方向偏移，那么波浪应该是均匀的。
让我检查是否是因为 numSegments 不够导致的锯齿效果...

或者问题是：波浪的起始和结束没有对齐到完整周期的边界。
sin(0) = 0, sin(waveCount * 2π) = 0 → 这是完整周期，应该没问题。

让我重新看截图：红色的波浪在右侧明显比左侧大。这可能是因为扇形角度很大（120°），
而波浪的径向偏移在不同角度位置产生了不同的视觉效果。

## 修复方案
- [ ] 波浪高度：amplitude = waveHeight / 2（使峰谷差等于用户输入值）
- [ ] 波浪分布：确保每个波浪周期占据相同的弧长
- [ ] 同步修改：DXF导出器、展开图可视化、3D立体图
