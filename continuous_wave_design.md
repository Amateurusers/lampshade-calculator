# 连续波浪曲线算法设计

## 问题分析

当前实现的问题：
- 圆弧之间有间隙（约150mm）
- 圆弧不是首尾相连的
- 左右边缘不对称

## 正确的实现目标

生成一条**连续的波浪曲线**，替代展开图的下口圆弧：
1. 所有圆弧首尾相连（端点重合）
2. 波谷和波峰圆弧在切点处相切（切线连续）
3. 左右边缘对称（相同的半径和波形相位）
4. 波浪数量由用户指定

## 算法设计

### 基本思路

将扇形的角度范围均分成 2N 段（N = 波浪数），每段交替使用波谷圆弧和波峰圆弧：
- 奇数段：波谷圆弧（向内凹）
- 偶数段：波峰圆弧（向外凸）

### 关键约束

1. **连续性**：每个圆弧的终点 = 下一个圆弧的起点
2. **相切性**：相邻圆弧在连接点处切线方向相同
3. **对称性**：第一段和最后一段是相同类型的圆弧（都是波谷或都是波峰）

### 几何参数

给定：
- 基准半径：R_base = 300mm（展开图外圆半径）
- 波谷圆半径：r_trough = 2.5mm（用户指定）
- 波峰圆半径：r_peak = 2.5mm（当前设置为与波谷相同）
- 扇形角度：θ_total（例如120°）
- 波浪数：N = 4

### 实现步骤

1. **均分角度**：
   - 每个半波的角度：Δθ = θ_total / (2N)
   - 第 i 个半波的角度范围：[θ_start + i*Δθ, θ_start + (i+1)*Δθ]

2. **计算圆弧参数**：
   - 对于波谷圆弧（i 为偶数）：
     * 圆心在基准圆内侧，距离原点 R_base - r_trough
     * 圆心角度：该半波的中心角度
   - 对于波峰圆弧（i 为奇数）：
     * 圆心在基准圆外侧，距离原点 R_base + r_peak
     * 圆心角度：该半波的中心角度

3. **计算圆弧起止点**：
   - 第一个圆弧的起点：在扇形的起始径向线上
   - 每个圆弧的终点 = 下一个圆弧的起点
   - 通过求解两圆的交点来确定连接点

4. **计算DXF圆弧角度**：
   - 将起止点坐标转换为相对于圆心的角度
   - 确保角度方向正确（逆时针）

## 关键问题：如何计算连接点？

两个相邻圆弧的连接点必须同时满足：
1. 在第一个圆弧上（距离第一个圆心 = r1）
2. 在第二个圆弧上（距离第二个圆心 = r2）
3. 在两个圆心连线的某个位置上（相切条件）

对于三圆相切的情况：
- 波谷圆、波峰圆、基准圆三者相切
- 连接点应该在基准圆上或附近

## 简化方案

为了确保连续性，可以采用以下简化方案：
1. 每个圆弧的起止点都在基准圆上（半径 = R_base）
2. 圆弧的圆心在基准圆的内侧或外侧
3. 通过调整圆心位置和圆弧角度，确保所有圆弧首尾相连

具体实现：
- 第 i 个半波的起始角度：α_i = θ_start + i*Δθ
- 第 i 个半波的结束角度：α_{i+1} = θ_start + (i+1)*Δθ
- 起点：(R_base * cos(α_i), R_base * sin(α_i))
- 终点：(R_base * cos(α_{i+1}), R_base * sin(α_{i+1}))
- 圆心：在该半波的中心角度上，距离原点 R_base ± r

这样可以确保所有圆弧的端点都在基准圆上，自然首尾相连。
