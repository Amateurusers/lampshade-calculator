# 两圆交点计算算法

## 问题

需要计算相邻两个圆弧的交点，确保它们首尾相连。

给定：
- 圆1：圆心 (cx1, cy1)，半径 r1
- 圆2：圆心 (cx2, cy2)，半径 r2

求：两圆的交点

## 算法

### 1. 计算两圆心之间的距离

```
d = sqrt((cx2 - cx1)^2 + (cy2 - cy1)^2)
```

### 2. 检查是否有交点

- 如果 d > r1 + r2：两圆不相交（距离太远）
- 如果 d < |r1 - r2|：一个圆在另一个圆内部
- 如果 d == 0 且 r1 == r2：两圆重合

### 3. 计算交点

如果两圆相交，交点位于两圆心连线的垂直平分线上。

```
a = (r1^2 - r2^2 + d^2) / (2 * d)
h = sqrt(r1^2 - a^2)

// 两圆心连线上的点P
px = cx1 + a * (cx2 - cx1) / d
py = cy1 + a * (cy2 - cy1) / d

// 两个交点
x1 = px + h * (cy2 - cy1) / d
y1 = py - h * (cx2 - cx1) / d

x2 = px - h * (cy2 - cy1) / d
y2 = py + h * (cx2 - cx1) / d
```

### 4. 选择正确的交点

对于波浪形灯罩，我们需要选择离原点更远的交点（波峰）或更近的交点（波谷）。

## 应用到波浪形灯罩

### 算法流程

1. 将扇形角度均分成 2N 段（N个波浪 = 2N个半波）
2. 对于每个半波 i：
   - 计算圆心位置：在该半波的中心角度上
   - 计算圆心到原点的距离：baseR ± radius
   - 如果 i > 0：计算与前一个圆弧的交点作为起点
   - 如果 i < 2N-1：计算与下一个圆弧的交点作为终点
3. 将起止点转换为相对于圆心的角度
4. 输出DXF圆弧

### 关键点

- 第一个圆弧的起点：在扇形的起始径向线上
- 最后一个圆弧的终点：在扇形的结束径向线上
- 中间的连接点：通过求解两圆交点得到

### 对称性保证

为了确保左右边缘对称：
- 第一个半波和最后一个半波应该是相同类型（都是波谷或都是波峰）
- 使用 2N 个半波（偶数个），第一个是波谷（i=0, 偶数），最后一个是波峰（i=2N-1, 奇数）
- 需要调整：让第一个和最后一个都是波谷，使用 2N+1 个半波？或者从波谷中点开始？

实际上，如果我们有N个完整波浪，每个波浪包含一个波谷和一个波峰，那么：
- 起点应该在波谷的中点
- 终点也应该在波谷的中点
- 这样左右边缘才对称

所以应该使用 2N 个半波，但起点和终点不在扇形的边缘，而是在第一个波谷和最后一个波谷的中点。
