# 顺序圆弧生成算法设计

## 问题分析

当前算法的问题：
- 预先计算所有波谷和波峰圆心的位置（均分扇形角度）
- 然后计算相邻圆弧的交点作为端点
- 但这导致圆弧之间有间隙，因为圆心位置是固定的，不能保证相邻圆弧相切

## 新算法思路

**核心思想：顺序生成圆弧，让每个圆弧适应前一个圆弧的终点**

### 算法步骤

1. **确定第一个圆弧的起点**
   - 起点在扇形的起始径向线上
   - 起点到原点的距离 = baseR - waveHeight/2（波谷最低点）

2. **确定第一个圆弧的圆心**
   - 第一个圆弧是波谷圆弧，半径 = r_trough
   - 圆心在起点的径向方向上，距离起点 r_trough
   - 圆心到原点的距离 = baseR - waveHeight/2 + r_trough

3. **计算第一个圆弧的终点**
   - 第一个圆弧（波谷）与第二个圆弧（波峰）相切
   - 第二个圆弧的圆心位置需要满足：
     a. 与第一个圆弧相切（两圆心距离 = r_trough + r_peak）
     b. 圆心到原点的距离 = baseR + waveHeight/2 - r_peak
   - 求解两圆的交点作为第一个圆弧的终点

4. **顺序生成后续圆弧**
   - 对于每个圆弧 i (i = 2, 3, ..., 2N):
     - 起点 = 前一个圆弧的终点
     - 圆心位置通过几何约束求解：
       * 与前一个圆弧相切
       * 圆心到原点的距离符合波谷或波峰的要求
     - 终点 = 与下一个圆弧的交点

5. **确保左右边缘对称**
   - 使用 2N+1 个半波
   - 第一个半波：从波谷最低点开始
   - 最后一个半波：到波谷最低点结束
   - 这样左右边缘都在相同的径向位置

## 关键几何计算

### 计算下一个圆心位置

给定：
- 当前圆弧的终点 P(x, y)
- 当前圆弧的半径 r_current
- 下一个圆弧的半径 r_next
- 下一个圆弧的类型（波谷或波峰）

求解：
- 下一个圆弧的圆心 C_next

约束条件：
1. |C_next - P| = r_next（圆心到起点的距离）
2. |C_next - O| = d_target（圆心到原点的距离）
   - 波谷：d_target = baseR - waveHeight/2 + r_trough
   - 波峰：d_target = baseR + waveHeight/2 - r_peak

这是两个圆的交点问题：
- 圆1：圆心P，半径r_next
- 圆2：圆心O（原点），半径d_target

### 计算圆弧的终点

给定：
- 当前圆弧的圆心 C_current
- 当前圆弧的半径 r_current
- 下一个圆弧的圆心 C_next
- 下一个圆弧的半径 r_next

求解：
- 两圆的交点（选择离原点较远的那个）

## 实现要点

1. **第一个圆弧特殊处理**
   - 起点在径向线上，可以直接计算

2. **最后一个圆弧特殊处理**
   - 终点应该在径向线上
   - 需要验证计算出的终点是否在正确的径向线上

3. **圆弧方向**
   - 所有圆弧应该按照逆时针方向绘制
   - 起始角度和终止角度需要正确计算

4. **数值稳定性**
   - 检查判别式是否为负（无解）
   - 检查圆弧是否退化为点或直线
